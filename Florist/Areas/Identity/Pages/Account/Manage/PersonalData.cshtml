@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Dane osobowe";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

    <div class="pl-4">
        <h4 class="brownColor">@ViewData["Title"]</h4>

        <div class="row">
            <div class="col-md-6 p-0">
                <p>Twoje konto zawiera dane osobowe, które nam przekazałeś. Ta strona umożliwia pobranie lub usunięcie tych danych.</p>
                <p>
                    <strong>Usunięcie tych danych spowoduje trwałe usunięcie Twojego konta, którego nie będzie można odzyskać.</strong>
                </p>
                <form id="download-data" asp-page="DownloadPersonalData" method="post" class="form-group">
                    <button class="goldButton" type="submit">POBIERZ</button>
                </form>
                <p>
                    <a id="delete" asp-page="DeletePersonalData" class="brownButton">USUŃ</a>
                </p>
            </div>
        </div>
    </div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}