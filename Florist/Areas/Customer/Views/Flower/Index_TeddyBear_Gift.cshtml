@model IEnumerable<Flower>
@using Florist.Utility
@{
    ViewData["Title"] = "Index";

}
    <div class="col-12 col-md-10 offset-md-1 mb-5">
        <div class="row">
            <div class="col-6">
                <h1 style="font-family:'Playfair Display', serif;" class="goldColor">Misie z róż</h1>
            </div>
            @if (User.IsInRole(SD.ManagerUser))
            {
                <div class="col-6 text-right pt-3">
                    <a asp-action="Create" class="goldButton">&nbsp;<i class="fas fa-plus"></i>&nbsp; DODAJ &nbsp;</a>
                </div>
            }
        </div>
        <hr />
        <partial name="IndexFlowerPartialView" />
    </div>